# Host-machine Prisma commands should use localhost because postgres is exposed
# from Podman Compose on 127.0.0.1:15432. Runtime containers keep using the
# Compose service hostname `postgres` from .env / docker-compose.yml.
DATABASE_URL=postgresql://jiying:jiying_dev_password@localhost:15432/jiying?schema=public
